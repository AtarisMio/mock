const development = {
    host        : 'localhost',
    user        : 'root',
    password    : 'root',
    database    : 'db_mock',
    tb_prefix   : 'mock'
};

const prodution = {
    // 写入生产配置
};

module.exports = {
    development,
    prodution
};
